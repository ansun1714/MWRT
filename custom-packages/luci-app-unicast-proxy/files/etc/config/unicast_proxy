# 单播 IPTV 代理配置
#
# global 'settings'：全局设置（DDNS 域名等）
# rule '<id>'：每条规则对应一条 socat TCP 转发
#   本机 listen_port  --->  upstream_host:upstream_port
#   wan_access=1 时自动在防火墙 WAN 区域放行 listen_port
#
# 通过 LuCI「服务 -> 单播IPTV代理」管理，无需手动编辑本文件

config global 'settings'
	option ddns_host ''
